/**
 * Размытие ФИО, контрагентов, названий проектов на экранах (NDA, публичные демо).
 * В интерфейсе переключателя нет — меняйте только здесь и пересоберите фронт (`npm run dev` / `npm run build`).
 * Для обычной работы со всеми подписями: `export const BLUR_PERSONAL_DATA = false`
 */
export const BLUR_PERSONAL_DATA = true
